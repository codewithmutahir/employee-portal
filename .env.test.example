# Copy this to .env.test and fill in real values before running Playwright tests.
# .env.test is git-ignored — never commit real credentials.

# The running Next.js dev server URL
BASE_URL=http://localhost:3000

# Admin / management account used for write operations
# (adding compensation events, exporting all employees, etc.)
TEST_ADMIN_EMAIL=mutharsoomro13@gmail.com   
TEST_ADMIN_PASSWORD=Mutahir#123

# Regular employee account used for self-service assertions
# (salary slip, salary changes card, self-export, clock-in)
TEST_EMPLOYEE_EMAIL=mutahir@elitesolutionusa.com    
TEST_EMPLOYEE_PASSWORD=Mutahir#123
